# Cara Menjalankan

1) Pastikan Python sudah terpasang.
2) Install dependency:
   ```bash
   pip install requests
   ```
3) Jalankan script:
   ```bash
   python main.py
   ```

Setelah selesai, file `users.csv` akan dibuat di folder yang sama.
